{
  "rules": {
    ".read": false,
    "foo": {
      ".read": false,
      ".write": false,
      "$bar": {
        ".read": "auth !== null",
        ".write": "auth.id === 1",
        "baz": {
          ".read": "root.child('users').child($bar).exists()"
        }
      }
    }
  }
}
